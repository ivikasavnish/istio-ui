apiVersion: v1
kind: Service
metadata:
  name: {{ include "meshcontrol.fullname" . }}
  labels:
    {{- include "meshcontrol.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "meshcontrol.selectorLabels" . | nindent 4 }}
