apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "meshcontrol.serviceAccountName" . }}
  labels:
    {{- include "meshcontrol.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
